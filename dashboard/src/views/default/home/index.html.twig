{% extends 'base.html.twig' %}

{% block content_title %}
    Sign In
{% endblock %}

{% block content %}
    <a href="{{ url('loginDetermine') }}" class="btn btn-primary btn-lg">Sign In</a>
    -or-
    <a href="{{ url('quick_buy_empty') }}" class="btn btn-success btn-lg">Sign Up</a>
{% endblock %}
